{
  "name": "@apad/env-tools",
  "version": "1.0.0",
  "description": "用来打包工具的process.env.*用的工具",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsup web.ts bundler.ts --clean --out-dir lib --dts --format esm,cjs,iife --global-name env-tools",
    "build:dependencies": "cd ../.. && turbo run build --filter=@apad/env-tools^..."
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
